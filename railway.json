{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "NIXPACKS", "buildCommand": "npm run build:production" },
  "deploy": {
    "startCommand": "npm run db:deploy && npm run start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}